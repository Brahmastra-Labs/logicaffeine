# Rust
target/
target-wasmaot/
# Cargo.lock IS committed: the e2e harness (crates/logicaffeine_tests/tests/common)
# seeds it into every generated project so dependency resolution is pinned to the
# exact versions the workspace tests against, and the published `largo` binary wants
# a locked build. A gitignored lockfile made CI's fresh checkout fail every e2e
# `seed_lockfile` with NotFound.

# Dioxus
dist/
.dioxus/

# Generated crate docs (built in CI)
apps/logicaffeine_web/public/crates/

# Node
node_modules/
.wrangler/

# OS
.DS_Store

# Dev artifacts
*.pdf
test_results*.txt
test-output*.log
# scripts/curriculum-review.sh output bundle
curriculum_bundle.txt

# Test run logs (scripts/run-all-tests.sh)
/logs/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Benchmarks (build artifacts and raw data; latest.json + history/ are tracked)
benchmarks/.logos-bench-target/
benchmarks/.ct/
benchmarks/bin/
benchmarks/generated/
# SAT arena: fetched/generated instances (regenerate via arena/fetch-sat.sh + gen-*.sh);
# the computed results (results/arena/*.json) and the scripts stay tracked.
benchmarks/arena/cache/
benchmarks/arena/instances/
benchmarks/results/raw/
benchmarks/results/raw-local/
benchmarks/results/raw-logos-vs-c/
benchmarks/results/local.json
benchmarks/results/local-logos-vs-c.json
benchmarks/results/c_results.json
benchmarks/results/c-baselines.json
benchmarks/results/*.bak
logs/
# asm/llvm-ir dumps (~620MB, regenerate via run-logos-vs-c.sh)
benchmarks/asm/

# Local cargo config (mold linker — not available in CI)
.cargo/

# External repos
AssertionForge/
Cirq/
FVEval/
VERT/
AssertionBench/

# Environment
.env
.env.local
.claude/
typescript
nanoGPT
nanochat

# SAT proof artifacts (emitted by logicaffeine_proof; regenerate via the emitter)
*.drat
*.lrat
*.dpr
*.dsr

# Locally-fetched external SAT solvers/checkers — never vendored into the repo
# (drat-trim, lrat-check, kissat, sadical, cadical, …); kept under /tmp or external-solvers/
external-solvers/
sadical/
drat-trim/
kissat/
cadical/
# The puzzles/ corpus is a large fetched/generated set — kept out of the repo —
# EXCEPT the single Simon fixture the phase_simon_* e2e tests read at runtime,
# which must be tracked or CI's checkout can't find it.
/puzzles/*
!/puzzles/3cf932ef3e458bf4fcba3081d831f5ae.json
apps/logicaffeine_nano
SimpleBench
# Web app runtime data bundle — generated by scripts/stage-web-data.sh, never committed
apps/logicaffeine_web/public/data/

# LOGOS chip campaign — confidential / patent-sensitive; never committed.
# Plans, generated PDFs, and the PDF generators. (PDFs also caught by *.pdf above;
# listed explicitly here so the chip set is self-documenting.) See work/TODO_LIST.md §0.
work/MAKE_A_CHIP.md
work/TODO_LIST.md
work/CHIP_MEMORY_BRIEF.pdf
work/CHIP_MEMORY_SPEC.pdf
scripts/_pdfmini.py
scripts/gen-chip-brief.py
scripts/gen-chip-spec.py
proofs/

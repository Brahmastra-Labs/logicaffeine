name: Deploy Docs

on:
  workflow_run:
    workflows: ["Tests"]
    types: [completed]
    branches: [main]
  workflow_dispatch:

env:
  # The `z3` crate links the system libz3 (no bundled feature); libz3-dev puts
  # the header here. cargo doc --workspace compiles verify/tv/synth, so the
  # docs build needs Z3 exactly like the test suite does.
  Z3_SYS_Z3_HEADER: /usr/include/z3.h

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy Rustdoc to Cloudflare Pages
    if: >
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success' &&
       github.event.workflow_run.event == 'push')
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Z3
        run: sudo apt-get update && sudo apt-get install -y z3 libz3-dev

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/
            ~/.cargo/git/
            target/
          key: ${{ runner.os }}-cargo-docs-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-docs-

      - name: Build documentation
        run: ./apps/logicaffeine_docs/build.sh

      - name: Install Wrangler
        run: npm install -g wrangler

      - name: Deploy to Cloudflare Pages
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          wrangler pages deploy apps/logicaffeine_docs/dist --project-name=logicaffeine-docs --branch=main --commit-dirty=true

# Changelog

All notable changes to this crate will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

This crate is an out-of-band tracing utility (`publish = false`, version pinned
at 0.0.0); it does not follow the workspace's lockstep version.

## [Unreleased]

### Added
- `wiki-trace` and `wiki-triage` binaries for tracing Wikipedia sentences through the NL→FOL pipeline and triaging parse failures.

{
  "name": "logicaffeine-wasm-node-test-deps",
  "private": true,
  "description": "WebSocket polyfill so the relay/interpreter wasm-bindgen tests run under node (no browser).",
  "dependencies": { "ws": "^8" }
}
